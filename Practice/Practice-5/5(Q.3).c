#include<stdio.h>
char str[30];
int le=0,i;
void main()
{
    printf("Enter string:");
    scanf("%s",&str);
    for(i = 0; i< str[i];i++);
    {
        le++;
    }
    for(i = le;i>=0;i--);
    {
        printf("%c\n",str[i]);
    }
    int main()
    {
        re();

        return 0;
    }
}
