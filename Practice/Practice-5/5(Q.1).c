//WAP find the length of a string without using library functions

#include<stdio.h>
int main()
{
    char str[1000];
    printf("Enter the string: ");
    scanf("%s",&str);

    for (i = 0; Str[i] != '\0'; i++);

    printf("Length of Str is %d ", i);
    return 0;
}
